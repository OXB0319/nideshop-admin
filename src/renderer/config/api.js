const rootUrl = 'http://127.0.0.1:8360/admin/';
// const rootUrl = 'http://flyinthesky.cn:8360/admin/';
const version = 'v0.0.7';
const isShowDelete = true; // 是否显示删除按钮，上线后的版本不显示
const api = {
    rootUrl:rootUrl,
    version:version,
    isShowDelete : isShowDelete,
};

export default api